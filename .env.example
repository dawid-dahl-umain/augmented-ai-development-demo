# Default environment values for acceptance testing
TEST_PROTOCOL=cli
PLAYWRIGHT_HEADLESS=true

# Web UI configuration (shared by acceptance tests and dev server)
UI_PORT=5173
UI_BASE_URL=http://localhost

